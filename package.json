{
  "name": "backend-test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "node dist/app.js",
    "compile": "rm -rf dist && npx tsc",
    "dev": "npm run compile && npm run start-server",
    "watch-server": "npx nodemon --watch 'src/**/*' -e ts,tsx --exec 'npx ts-node' ./src/app.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.2",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.17",
    "typeorm-naming-strategies": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/pg": "^8.10.2",
    "typescript": "^5.1.6"
  }
}
